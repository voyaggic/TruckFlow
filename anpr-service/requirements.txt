# Core (required) — frame capture + preview
numpy>=1.24
opencv-python>=4.8

# OCR backends — install at least one for real plate reading.
# Without any of these the service runs in deterministic mock mode
# (pipeline still works end-to-end, but plates are synthetic).
paddlepaddle>=2.5  # PaddleOCR runtime (CPU-only build — no CUDA needed)
paddleocr>=2.7     # PaddleOCR engine (downloads models on first use)
# easyocr>=1.7     # alternative: EasyOCR (requires PyTorch ~1.5 GB)
pygrabber>=0.2
